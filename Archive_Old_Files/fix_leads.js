const fs = require("fs"); let code = fs.readFileSync("new_features.js", "utf8"); let start = code.indexOf("// --- LEADS ---"); let end = code.indexOf("window.handleTargetImport"); if(start !== -1 && end !== -1) { let newCode = code.substring(0, start) + fs.readFileSync("leads_new.js", "utf8") + "\n  " + code.substring(end); fs.writeFileSync("new_features.js", newCode, "utf8"); console.log("Done!"); } else { console.log("Not found"); }
